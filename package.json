{
  "name": "symbios",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "test": "pnpm -r test",
    "build:web": "pnpm --filter frontend build",
    "lint": "echo 'add eslint here' && exit 0"
  }
}
